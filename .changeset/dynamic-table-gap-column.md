---
"@crbroughton/recul": minor
---

Add `gap` column showing number of stable versions between installed and latest. Refactor table to use dynamic column widths based on content. Add `--behindBehavior` CLI flag and `behind-behavior` action input to override config at runtime.
